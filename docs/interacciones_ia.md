
# Registro de Consultas a Herramientas de IA

[Ejemplo 1: Generación de la estructura base (FSM)

Pregunta:
Actúa como un Ingeniero de Software experto en Python. Necesito diseñar la estructura base de una Máquina de Estados Finitos (FSM) para un chatbot de WhatsApp que gestiona vacaciones corporativas. El flujo se comunica vía Webhooks. Define los estados iniciales y cómo se estructuraría la persistencia en memoria por cada user_id.

Ejemplo 2: Gestión del "Camino Infeliz" (Validación del Legajo)
Pregunta:
Continuando con el código anterior, muestra cómo manejar el cambio de estado cuando el usuario está en ESTADO_ESPERANDO_LEGAJO. Añade el control de excepciones (Unhappy Path) por si el usuario introduce letras o un texto inválido en lugar de un número entero, respondiendo con un mensaje de error sin romper la FSM.

Ejemplo 3: Recepción y desempaquetado del Webhook de WhatsApp
Pregunta:
Diseña una función rápida en Python que emule la recepción del JSON (Payload) que envía la API de WhatsApp Business mediante su Webhook, extrayendo el número de teléfono del usuario (from) y el texto del mensaje (body).
]
